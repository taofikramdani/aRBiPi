<x-app-layout><x-slot:header>Paket Tryout</x-slot:header><div class="mb-5 flex justify-end"><a class="btn-primary" href="{{ route('admin.tryouts.create') }}">Buat Paket</a></div><div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@foreach($tryouts as $t)<div class="card"><span class="text-xs font-bold uppercase text-indigo-600">{{ $t->subject?->name ?? 'Campuran' }}</span><h2 class="mt-2 text-lg font-bold">{{ $t->title }}</h2><p class="my-4 text-sm text-slate-500">{{ $t->questions_count }} soal · {{ $t->attempts_count }} peserta · {{ $t->duration_minutes }} menit</p><a class="btn-secondary" href="{{ route('admin.tryouts.edit',$t) }}">Kelola</a></div>@endforeach</div><div class="mt-5">{{ $tryouts->links() }}</div></x-app-layout>
