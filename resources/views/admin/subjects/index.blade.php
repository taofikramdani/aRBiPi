<x-app-layout><x-slot:header>Mata Pelajaran</x-slot:header><div class="mb-5 flex justify-end"><a class="btn-primary" href="{{ route('admin.subjects.create') }}">Tambah Mata Pelajaran</a></div><div class="table-wrap"><table class="table"><thead><tr><th>Nama</th><th>Soal</th><th>Tryout</th><th>Aksi</th></tr></thead><tbody>@foreach($subjects as $s)<tr><td><b>{{ $s->name }}</b><p class="text-slate-500">{{ $s->description }}</p></td><td>{{ $s->questions_count }}</td><td>{{ $s->tryouts_count }}</td><td><a class="text-indigo-600" href="{{ route('admin.subjects.edit',$s) }}">Edit</a></td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $subjects->links() }}</div></x-app-layout>
