<x-app-layout><x-slot:header>{{ $subject->exists ? 'Edit' : 'Tambah' }} Mata Pelajaran</x-slot:header><form class="card max-w-2xl space-y-5" method="POST" action="{{ $subject->exists ? route('admin.subjects.update',$subject) : route('admin.subjects.store') }}">@csrf @if($subject->exists)@method('PUT')@endif<div><label class="label">Nama</label><input class="input" name="name" value="{{ old('name',$subject->name) }}" required></div><div><label class="label">Slug</label><input class="input" name="slug" value="{{ old('slug',$subject->slug) }}" required></div><div><label class="label">Deskripsi</label><textarea class="input" name="description" rows="4">{{ old('description',$subject->description) }}</textarea></div><label class="flex gap-2"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$subject->is_active ?? true))> Aktif</label><button class="btn-primary">Simpan</button></form></x-app-layout>
