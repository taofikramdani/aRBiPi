<x-app-layout><x-slot:header>Tryout Tersedia</x-slot:header><div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($tryouts as $t)<div class="card"><p class="text-xs font-bold uppercase text-indigo-600">{{ $t->subject?->name ?? 'Campuran' }}</p><h2 class="mt-2 text-xl font-bold">{{ $t->title }}</h2><p class="my-4 text-sm text-slate-500">{{ $t->questions_count }} soal · {{ $t->duration_minutes }} menit</p><form method="POST" action="{{ route('student.tryouts.start',$t) }}">@csrf<button class="btn-primary">Mulai Sekarang</button></form></div>@empty<div class="card">Belum ada tryout tersedia.</div>@endforelse</div><div class="mt-5">{{ $tryouts->links() }}</div></x-app-layout>
